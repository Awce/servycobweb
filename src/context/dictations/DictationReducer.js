import { SELECCIONAR_CLIENTE, SELECCIONAR_DAMA } from "../../types";

export default (state, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
      break;
  }
};
